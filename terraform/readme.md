For running the terraform in local, go under terraform/environment(dev, prod) :
    - az login with your Azure account
    - az account set -s "your-azure-subscription-id"
    - terraform init
    - terraform plan
    - terraform apply